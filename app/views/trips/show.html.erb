<%# Meta %>
<% content_for :meta_title, "Trip for #{@trip.destination}" %>
<%# <% content_for :meta_image, cl_image_path(@trip.photo.key) if @trip.photo.attached? %>

<%# Banner %>
<%= render "layouts/trip_banner" %>

<div class="container">
  <%# Days %>
  <%= render "trips/days" %>

  <%= link_to "Delete this trip", delete_path(@trip), data: { turbo_method: :delete }, class: "delete" %>
</div>

<%= render "trips/navbartrips" %>
